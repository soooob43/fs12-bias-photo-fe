import LoginForm from './(components)/LoginForm';
import Logo from '../(components)/Logo';

const page = () => {
  return (
    <main>
      <Logo />
      <LoginForm />
    </main>
  );
};

export default page;
